name: Read File Contents (Docker Version)

on:
    workflow_dispatch: 

jobs:
  read_file_contents:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Call reusable action
        uses: SAG-Trial/QM/.github/actions/GetPwdFiler@main